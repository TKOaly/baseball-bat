/*import { Config } from "./config";

export interface Module<Deps extends Record<string, Module> = never> {
  id: string;
  start(deps: Deps): void;
}

export interface ModuleFactory<M extends Module> {
  (config: Config): Promise<M>;
}*/
